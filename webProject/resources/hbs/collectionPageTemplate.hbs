<!DOCTYPE html>
<header></header>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/index.css" rel="stylesheet">
    <link href="/forms.css" rel="stylesheet">
    <link href="/profile.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Jost:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">
    <!--<script defer type="module" src="/edit.js"></script>-->
    <title>Museonline, Edit profile</title>
</head>
<body style="background-color: rgba(0,0,0,0.2); background-image: none;">
    <div style="height: 100vh; width: 100vw;" class="center-container escape-on-click">
        <div style="height: 90%; width: calc(75% + 6em); overflow: auto; background-color: #F2F2EB; padding: 0px;" class="default-container">
            <div class="scrollable text-fading" style="padding: 3em; border-radius: inherit;">
                <div style="display: flex; flex-direction: row; justify-content: center; align-items: center; width: 100%;">
                    <div class="text-title">{{collectionName}}</div>
                </div>
                <div class="collections-grid" style="width: 100%; box-sizing: content-box;">
                    {{#each pictures}}
                    <div id="{{this.id}}" class="collection-container" style="height: fit-content; width: 300px;">
                        <div class="rounded-btn-white" id="delete">
                            <img src="/close.svg" alt="Delete picture" id="delete">
                        </div>
                        <div class="user-picture-view" style="background-image: url('{{this.imageUrl}}'); border-radius: inherit;"></div>
                        <a class="text-primary-bold" style="word-wrap: break-word;">{{this.name}}</a>
                        <a class="text-secondary" style="word-wrap: break-word;">{{this.description}}</a>
                    </div>
                    {{/each}}
                </div>
            </div>
        </div>
    </div>
</body>
</html>